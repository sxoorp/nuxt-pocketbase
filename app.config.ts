export default defineAppConfig({
    ui: {
        primary: "blue",
        gray: "zinc",
        button: {
            rounded: "rounded-sm"
        },
        input: {
            rounded: "rounded-sm"
        }
    }
});